// code = `${designator}.${station}`, subCode = `${issued}`
// code = URNT15.KNHC, subCode = 141852

// Except for NOUS42
// code = `${designator}.${station}`, subCode = `${tcpodNumber}`
// code = NOUS42.KNHC, subCode = 24-160